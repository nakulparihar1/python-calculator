# 🧮 Simple Python Calculator (CLI)

This project is a basic command-line calculator built using Python. It allows users to perform addition, subtraction, and multiplication in a loop until they choose to exit.

## 📌 Features

* Repeated menu inside a loop
* Supports:

  * Addition
  * Subtraction
  * Multiplication
* Exits automatically when option **4** is selected
  
## 🚀 How to Run

1. Save the file as `calculator.py`.
2. Run it using:

   ```bash
   python calculator.py
   ```

## 🛠️ Future Improvements

* Add division functionality
* Add error-handling (invalid input)
* Add continuous calculation without re-entering numbers
* Add modulo, power, etc.

## 📄 License

This project is open-source and free to use.
